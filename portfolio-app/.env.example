# Public site URL, used for metadata / Open Graph. No trailing slash.
NEXT_PUBLIC_SITE_URL=https://faruk-duman.example

# Optional: contact-form endpoint (wired in a later phase, if you choose to add one).
# CONTACT_FORM_ENDPOINT=
